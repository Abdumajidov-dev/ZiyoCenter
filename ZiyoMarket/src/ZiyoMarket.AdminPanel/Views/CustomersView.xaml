<UserControl x:Class="ZiyoMarket.AdminPanel.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Mijozlar" FontSize="26" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryHueDarkBrush}"/>
                <TextBlock FontSize="13" Foreground="Gray" Margin="0,2,0,0">
                    <Run Text="Jami:"/>
                    <Run Text="{Binding TotalCount}" FontWeight="SemiBold"/>
                    <Run Text="ta mijoz"/>
                </TextBlock>
            </StackPanel>
            <Button Grid.Column="1"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding LoadCustomersCommand}"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center"/>
                    <TextBlock Text="Yangilash" Margin="6,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Search & Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     materialDesign:HintAssist.Hint="Ism, telefon bo'yicha qidirish..."
                     materialDesign:HintAssist.IsFloating="True"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,12,0">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <ComboBox Grid.Column="1"
                      materialDesign:HintAssist.Hint="Holat"
                      materialDesign:HintAssist.IsFloating="True"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      SelectedItem="{Binding StatusFilter}"
                      Margin="0,0,12,0">
                <ComboBoxItem>Barchasi</ComboBoxItem>
                <ComboBoxItem>Faol</ComboBoxItem>
                <ComboBoxItem>Faol emas</ComboBoxItem>
            </ComboBox>

            <Button Grid.Column="2" Command="{Binding SearchCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Height="44" Padding="20,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center"/>
                    <TextBlock Text="Qidirish" Margin="6,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- DataGrid -->
        <Grid Grid.Row="2">
            <materialDesign:Card Padding="0">
                <DataGrid ItemsSource="{Binding Customers}"
                          SelectedItem="{Binding SelectedCustomer}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F0F0F0"
                          RowHeight="54"
                          FontSize="13">

                    <DataGrid.Columns>

                        <!-- Avatar + Name -->
                        <DataGridTemplateColumn Header="Mijoz" Width="220" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                                        <Border Width="36" Height="36" CornerRadius="18"
                                                Background="{DynamicResource PrimaryHueMidBrush}">
                                            <TextBlock Text="{Binding Initials}" Foreground="White"
                                                       FontWeight="Bold" FontSize="13"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                        <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FullName}"
                                                       FontWeight="SemiBold" FontSize="13"/>
                                            <TextBlock Text="{Binding Phone}"
                                                       Foreground="Gray" FontSize="11"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Status Badge -->
                        <DataGridTemplateColumn Header="Holat" Width="110" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="10,4"
                                            HorizontalAlignment="Left" Margin="8,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Background" Value="#FFEBEE"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding StatusText}"
                                                   FontSize="12" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                            <Setter Property="Foreground" Value="#C62828"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Orders -->
                        <DataGridTemplateColumn Header="Buyurtmalar" Width="115" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                                VerticalAlignment="Center" Margin="8,0">
                                        <materialDesign:PackIcon Kind="Receipt" Width="15" Height="15"
                                                                 Foreground="Gray"
                                                                 VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding EffectiveOrders}"
                                                   Margin="6,0,0,0" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Cashback -->
                        <DataGridTemplateColumn Header="Cashback" Width="145" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                                VerticalAlignment="Center" Margin="8,0">
                                        <materialDesign:PackIcon Kind="CashMultiple" Width="15" Height="15"
                                                                 Foreground="#FF8F00"
                                                                 VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding FormattedCashback}"
                                                   Margin="6,0,0,0"
                                                   Foreground="#E65100"
                                                   FontWeight="SemiBold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Total Spent -->
                        <DataGridTemplateColumn Header="Jami sarfladi" Width="145" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FormattedTotalSpent}"
                                               VerticalAlignment="Center" Margin="8,0"
                                               Foreground="#1565C0" FontWeight="SemiBold"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Joined -->
                        <DataGridTextColumn Header="A'zo sana" Width="115"
                                            Binding="{Binding CreatedAt}" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="8,0"/>
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="FontSize" Value="12"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Action Button -->
                        <DataGridTemplateColumn Header="" Width="100" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Batafsil"
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Foreground="{DynamicResource PrimaryHueMidBrush}"
                                            FontWeight="SemiBold" FontSize="12"
                                            Command="{Binding DataContext.OpenDetailCommand,
                                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <!-- Loading Overlay -->
            <Border Background="#80FFFFFF"
                    Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                 Width="48" Height="48" IsIndeterminate="True"/>
                    <TextBlock Text="Yuklanmoqda..." Margin="0,10,0,0"
                               HorizontalAlignment="Center" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Pagination -->
        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center"
                       Foreground="Gray" FontSize="13">
                <Run Text="Sahifa"/>
                <Run Text="{Binding CurrentPage}" FontWeight="Bold"/>
                <Run Text="/"/>
                <Run Text="{Binding TotalPages}" FontWeight="Bold"/>
            </TextBlock>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding PreviousPageCommand}"
                        Margin="0,0,8,0" Padding="14,6">
                    <materialDesign:PackIcon Kind="ChevronLeft"/>
                </Button>
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding NextPageCommand}"
                        Padding="14,6">
                    <materialDesign:PackIcon Kind="ChevronRight"/>
                </Button>
            </StackPanel>

            <TextBlock Grid.Column="2" HorizontalAlignment="Right"
                       VerticalAlignment="Center" Foreground="Gray" FontSize="13">
                <Run Text="Jami"/>
                <Run Text="{Binding TotalCount}" FontWeight="Bold"/>
                <Run Text="ta yozuv"/>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>
